import React from 'react';
import {FontAwesomeIcon} from "@fortawesome/react-fontawesome";
import {faEdit, faEyeDropper, faSave, faTrashCan} from "@fortawesome/free-solid-svg-icons";

export default function CurrentSpace({space}) {
    return (
        <div style={{display: 'flex', flexDirection: 'column'}}>
            <div style={listStyle}>
                </div>
        </div>
    );
}